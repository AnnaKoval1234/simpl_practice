Возможно ли создать функции A и B, чтобы new A() == new B()?

function A() { ... }
function B() { ... }

let a = new A();
let b = new B();

alert( a == b ); // true
Если да – приведите пример вашего кода.

Ответ: нет, не возможно. -- Оказывается, возможно. Надо сделать так, чтоб a и b возвращали ссылку на один и тот же объект. 
Можно создать этот объект заранее.