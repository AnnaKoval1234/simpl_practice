Можно ли изменить объект, объявленный с помощью const? Как вы думаете?

const user = {
  name: "John"
};

// это будет работать?
user.name = "Pete";

Ответ: сам объект, объявленный с помощью const, изменить нельзя. Его свойства - можно. Присваивание свойству name 
значение "Pete" работать будет.